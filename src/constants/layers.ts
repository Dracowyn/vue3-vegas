/**
 * 组件内部的层叠顺序。
 *
 * 所有图层都是同一个根容器（未创建层叠上下文）的兄弟节点，光靠 DOM 顺序
 * 不足以表达覆盖关系——进入中的幻灯片必须盖住正在离场的那张，而遮罩与进度条
 * 又必须盖住已经就位的幻灯片。因此统一在这里声明各层的 z-index。
 */
export const VEGAS_LAYERS = {
	/** 默认背景图，位于所有幻灯片之下 */
	defaultBackground: 0,
	/** 正在离场的幻灯片 */
	slideLeaving: 0,
	/** 正在进入 / 已就位的幻灯片 */
	slideEntering: 1,
	/** 半透明遮罩，必须盖在幻灯片之上 */
	overlay: 2,
	/** 顶部进度条，压在遮罩之上 */
	timer: 3,
	/** 加载指示器，永远在最上层 */
	loader: 10,
} as const;
